# @package _global_

defaults:
  - _self_
  - data: nyu
  - model: unet
  - callbacks: default
  - logger: null
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
